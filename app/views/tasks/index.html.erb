<h1>Tasks</h1>

<ul>
  <% @task.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.status %> &gt; <%= task.content %></li>
  <% end %>
</ul>

<%= paginate @tasks %>

<%= link_to 'Create New Task', new_task_path %>